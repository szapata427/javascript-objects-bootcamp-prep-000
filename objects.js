var playlist = { maluma: "corazon" };

function updatePlaylist(obj, key, value) {
  
  playlist[reykon] = "mala"
  
  return playlist
}


function updatePlaylist(playlist, key, value) {
  playlist[key] = value
 
  return playlist
}

const playlist = { mauluma: corazon}

updatePlaylist(playlist, 'reykon', 'mala')
